<busconfig>
  <policy user="root">
    <allow own="com.blik.nodesManager"/>

    <!-- Allow all methods on interfaces -->
    <allow send_destination="com.blik.nodesManager"/>
  </policy>
</busconfig>
